generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

model product {
  id        Int     @id @default(autoincrement())
  faction   String
  character String
  fact      String
  image     String?
}
